{% extends 'base.html.twig' %}

{% block title %}Offres de Projets{% endblock %}

{% block body %}
    <h1>Offres de projets</h1>

    {% if offres is empty %}
        <p>Aucune offre de projets créer pour le moment.</p>
    {% else %}
        {% for offre in offres %}
            <div class="card">
                <div class="card-header">
                    {{ offre.titre }}
                </div>
                <div class="card-body">
                    <h5 class="card-title">Le type d'offre: {{ offre.reftypeOffre.nom}}</h5>
                    <p class="card-text">Description de l'offre : {{ offre.description }}</p>
                    <p class="card-text">Les missions lier: {{ offre.missionlier }}</p>
                    <p class="card-text">Le salaire: {{ offre.salaire}}</p>
                    <a href="{{ path('participerOffre', { 'id': offre.id }) }}">Participer</a>
                </div>
            </div>
        {% endfor %}
    {% endif %}
{% endblock %}